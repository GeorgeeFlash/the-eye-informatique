// Product editor (M2.2)
export default function AdminProductEditorPage({
  params,
}: {
  params: Promise<{ id: string }>
}) {
  void params
  return (
    <div>
      {/* TODO: Product form — name, brand, price, variants, images, AI description */}
    </div>
  )
}
