// Blog article editor (M6.1)
export default function AdminBlogEditorPage({
  params,
}: {
  params: Promise<{ id: string }>
}) {
  void params
  return (
    <div>
      {/* TODO: BlockNote editor, title, cover image, status toggle */}
    </div>
  )
}
