// Order detail + status management (M3.3)
export default function AdminOrderDetailPage({
  params,
}: {
  params: Promise<{ id: string }>
}) {
  void params
  return (
    <div>
      {/* TODO: Order details, items, payment status, fulfillment branch, status history */}
    </div>
  )
}
