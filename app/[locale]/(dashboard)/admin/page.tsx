// Admin dashboard home — role-adaptive (M8.1)
// Branch Admin: branch KPIs, recent orders, repair queue snapshot
// Central Admin: cross-branch KPIs, branch selector
export default function AdminDashboardPage() {
  return (
    <div>
      {/* TODO: Role-adaptive stats cards, recent orders, repair queue snapshot */}
    </div>
  )
}
