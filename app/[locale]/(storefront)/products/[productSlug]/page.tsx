// Product detail page (M2.1)
export default function ProductDetailPage({
  params,
}: {
  params: Promise<{ productSlug: string }>
}) {
  void params
  return (
    <div>
      {/* TODO: Product gallery, details, variants, add to cart, AI description */}
    </div>
  )
}
